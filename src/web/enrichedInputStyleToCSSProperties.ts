import type { CSSProperties } from 'react';
import type { EnrichedInputStyle } from '../types';

export function enrichedInputStyleToCSSProperties(
  _style: EnrichedInputStyle
): CSSProperties {
  return {};
}
