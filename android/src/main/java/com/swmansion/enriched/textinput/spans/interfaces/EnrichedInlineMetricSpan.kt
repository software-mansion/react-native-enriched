package com.swmansion.enriched.textinput.spans.interfaces

/**
 * Interface for inline spans that extend MetricAffectingSpan
 * (e.g. bold, italic, inline code). This interface is used by EnrichedSpanWatcher to
 * detect spans and trigger the necessary layout invalidation.
 */
interface EnrichedInlineMetricSpan
