package com.swmansion.reactnativerichtexteditor.spans.interfaces

// Spans that are prefixed with zero-width space (\u200B) to ensure that they are visible even when empty.
interface EditorZeroWidthSpaceSpan {
}
